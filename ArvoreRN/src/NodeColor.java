/* O enum NodeColor tem a função de representar as cores dos nós em uma árvore rubro-negra. Cada
nó pode ser colorido de duas maneiras: vermelho (RED) ou preto (BLACK).
 */

public enum NodeColor {
    RED, BLACK;
}
